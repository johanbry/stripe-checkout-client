import "./header.css";

type Props = {};

const Header = (props: Props) => {
  return (
    <header>
      <div className="header-container">
        <h1>Ball Store</h1>
      </div>
    </header>
  );
};

export default Header;
